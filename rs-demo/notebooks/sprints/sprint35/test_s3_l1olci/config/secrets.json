{
    "s3_cluster": {
        "comment": "Use the cluster S3 bucket for both cluster and local mode",
        "key": "${S3_ACCESSKEY_CLUSTER}",
        "secret": "${S3_SECRETKEY_CLUSTER}",
        "endpoint_url": "${S3_ENDPOINT_CLUSTER}",
        "region_name": "${S3_REGION_CLUSTER}"
    },
    "s3_local_or_cluster": {
        "comment": "Use either the local (seaweedfs) or cluster S3 bucket, depending on the mode",
        "key": "${S3_ACCESSKEY}",
        "secret": "${S3_SECRETKEY}",
        "endpoint_url": "${S3_ENDPOINT}",
        "region_name": "${S3_REGION}"
    }
}
